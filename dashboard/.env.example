# Base URL of the FastAPI backend (predict_api.py). No trailing slash.
# Copy this file to .env.local for local overrides — Vite loads it automatically
# and it's already git-ignored.
VITE_API_BASE_URL=http://127.0.0.1:8000
